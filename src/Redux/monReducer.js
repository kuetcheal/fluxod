
// monReducer.js
const initialState = {
    // Définissez l'état initial ici
  };
  
  const monReducer = (state = initialState, action) => {
    switch (action.type) {
      // Définissez les gestionnaires d'actions ici
      default:
        return state;
    }
  };
  
  export default monReducer;
  